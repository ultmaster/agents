interface:
  display_name: "UI Work"
  short_description: "Design, build, and prove UI changes"
  default_prompt: "Use $ui-work to design this UI change, implement it, and prove the result in the running interface."
